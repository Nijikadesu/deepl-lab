## 任务一

- 安装配置深度学习环境，安装 `torch`, `torchvision` 库
- 阅读并运行 `layers.py`，学习神经网络中常见模块的实现。

Pytorch 文档：https://pytorch.org/docs/stable/index.html

## 任务二

在 MNIST 数据集上实现 LeNet 的训练和推导。

- 按照提示和要求，将 `model.py` 中的 `???` 部分补全，然后运行代码，检查模型实现。
- 按照提示和要求，将 `train.py` 中的 `???` 部分补全，然后运行代码，进行模型训练并查看结果。

## 完成情况

> [!NOTE]
> 目前 `model.py` 的实现过不了 validation，数值精度存在微小差异，这可能由于模块声明或者硬件差异导致，具体问题排查等待实验具体说明。